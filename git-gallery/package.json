{
  "name": "git-gallery",
  "private": true,
  "type": "module",
  "description": "Optional isomorphic-git install for local web demos (CDN fallback in assets/git-web.js)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "isomorphic-git": "1.30.1"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
