# Init

Creates a throwaway repository with `GitRepo::init`.

- **Desktop:** `gix::init` under a temp directory.
- **Web:** isomorphic-git `init` under OPFS (`/repos/...`).

Press **Init** to allocate a fresh workdir and initialize `.git`.
